import Foundation
import UIKit

extension R.Storyboard {
    public struct Loan {
        public struct Completion {
            public static var ViewController: R.Storyboard { .init(name: "LoanCompletionViewController") }
        }
    }
}
